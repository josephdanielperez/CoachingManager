<% if logged_in? %> 
    <% if is_users_appointment?(@appointment) %> 
        <%= render 'errors' %>
        <h1> Book a Training Session </h1> 

        <%= render 'form' %>

    <% end %> 
<% end %>