<% if @user %>
    <% if @user.appointments.empty? %>
        <%= @user.name %> has not booked any training sessions yet.
    <% else %>
        <%= render 'appt' %>
    <% end %>
<% else %>
    <%= render 'appt' %>
<% end %>