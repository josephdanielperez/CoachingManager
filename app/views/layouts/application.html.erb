<!DOCTYPE html>
<html>
  <head>
    <title>Coaching Manager (Appointment Manager System)</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%#= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="text">
    Coaching Manager // 
    <% if current_user %>
      <!-- current_user will return true if a user is logged in -->
      <%= "Logged in as #{current_user.email}" %> • <%= link_to 'Home', root_path %> • <%= link_to 'Profile', user_path(current_user) %> • <%= link_to 'Log Out', logout_path, method: :delete %>
    <% else %>
      <!-- not logged in -->
      <%= link_to 'Home', root_path %> • <%= link_to 'Log In', login_path %> or <%= link_to 'Sign Up', new_user_path %>
    <% end %>
    <hr>
    </div>

    <% if flash[:notice] %> 
      <%= flash[:notice] %> 
    <% end %> <br>

    <%= yield %>
  </body>
</html>